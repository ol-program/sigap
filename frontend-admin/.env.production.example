# Salin jadi frontend-admin/.env.production di VPS (TIDAK di-commit, lihat
# root .gitignore) -- Vite otomatis pakai file ini saat `npm run build`.
# SAMA dengan frontend/.env.production (satu backend, dua frontend).
VITE_API_BASE_URL=https://REPLACE_WITH_API_DOMAIN
