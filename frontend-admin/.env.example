# Alamat backend API (backend/api/main.py) -- SAMA dengan yang dipakai
# frontend/ (satu backend, dua frontend terpisah). Salin file ini jadi .env
# lalu sesuaikan kalau API tidak jalan di localhost:8000.
VITE_API_BASE_URL=http://localhost:8000
